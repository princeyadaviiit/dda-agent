# Procfile for Railway
# This tells Railway how to run different processes

# Web service (dashboard - always on)
web: node dashboard/server.js

# Worker service (bot - can be triggered manually or via cron)
worker: node bot.js
